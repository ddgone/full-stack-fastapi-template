# 域名配置
# 部署到生产环境时，需通过环境变量设置为实际域名
# 用于 Traefik 传输流量和获取 TLS 证书
DOMAIN=localhost
# 如需测试本地 Traefik 配置，可取消下方注释
# DOMAIN=localhost.tiangolo.com

# 后端生成邮件中的链接时，指向前端的地址
FRONTEND_HOST=http://localhost:5173
# 在预发布（staging）和生产（production）环境中，需设置为实际前端域名，例如：
# FRONTEND_HOST=https://dashboard.example.com

# 环境标识：local（本地开发）、staging（预发布）、production（生产）
ENVIRONMENT=local

# 项目名称，将显示在 API 文档和邮件中
PROJECT_NAME="全栈 FastAPI 项目"
# Docker Compose 堆栈名称，用于标签和项目标识（不含空格）
STACK_NAME=full-stack-fastapi-project

# 后端配置
# 允许的跨域请求源，多个源用逗号分隔
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173,http://localhost.tiangolo.com"
# 后端安全密钥，用于签名 JWT 等令牌（必须替换默认值）
SECRET_KEY=jlWpAz--Lkdgi7Lsf1dF0DxU_Ks1jvPi7qGDcR4Big0
# 初始超级管理员邮箱
FIRST_SUPERUSER=admin@example.com
# 初始超级管理员密码（必须替换默认值）
FIRST_SUPERUSER_PASSWORD=B753Prlg2EACeh6vfbvlsQKAP_x1vb8XXVAIpSY3GYw

# 邮件服务配置
# SMTP 服务器主机（如 Mailgun、Sendgrid 等，本地开发可留空）
SMTP_HOST=
# SMTP 服务器用户名
SMTP_USER=
# SMTP 服务器密码
SMTP_PASSWORD=
# 发送邮件的邮箱地址
EMAILS_FROM_EMAIL=info@example.com
# 是否启用 SMTP TLS 加密
SMTP_TLS=True
# 是否启用 SMTP SSL 加密（与 TLS 二选一）
SMTP_SSL=False
# SMTP 服务器端口
SMTP_PORT=587

# PostgreSQL 数据库配置
# 数据库服务器地址（本地开发默认 localhost，Docker 环境可能为 db）
POSTGRES_SERVER=localhost
# 数据库端口
POSTGRES_PORT=5432
# 数据库名称
POSTGRES_DB=app
# 数据库用户名
POSTGRES_USER=postgres
# 数据库密码（必须替换默认值）
POSTGRES_PASSWORD=KeF0Rm595bRQEfIbKa5tBF9I7ZnHPd8TwvugrN_52-4

# Sentry 错误监控 DSN（不使用可留空）
SENTRY_DSN=

# 配置自定义 Docker 镜像仓库的镜像名称
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend